repo: caarlos0/twatcher
binary_name: twatcher
brew:
  repo: caarlos0/homebrew-tap
build:
  oses:
    - windows
    - linux
    - darwin
archive:
  format: zip
  name_template: "{{.BinaryName}}_{{.Version}}_{{.Os}}_{{.Arch}}"
